# 工程名称
workspace "SHWallet.xcworkspace"
# App工程名称
xcodeproj "SHWallet.xcodeproj"

# 最低要求版本
platform :ios, "8.0"
# 去除pod库警告
#inhibit_all_warnings!

# use_frameworks!

# pod库只存在当前目标
# pod库只存在当前目标
target :"SHWallet" do
pod 'Masonry', '~> 1.1.0'
end

